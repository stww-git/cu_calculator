========================================
  워크플로우 업데이트 완료
========================================

❌ 오류 내용:
- actions/upload-artifact@v3가 deprecated됨

✅ 수정 사항:
- actions/upload-artifact@v3 → v4로 업데이트
- actions/checkout@v3 → v4로 업데이트  
- actions/setup-python@v4 → v5로 업데이트

📝 다음 단계:

1. GitHub 저장소에서 `.github/workflows/build_windows.yml` 파일 열기

2. 파일 내용을 아래로 교체:

```yaml
name: Build Windows Executable

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'calculator_cu.py'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: Install PyInstaller
      run: pip install pyinstaller
    
    - name: Build executable
      run: |
        pyinstaller --onefile --name "CU발주시스템" --console calculator_cu.py
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: CU발주시스템_Windows
        path: dist/CU발주시스템.exe
```

3. "Commit changes" 클릭하여 저장

4. Actions 탭에서 워크플로우 다시 실행

========================================

